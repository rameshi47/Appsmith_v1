SELECT
  *
FROM
  public.employees
WHERE
  "employee_name" ilike '%{{data_table.searchText}}%'
ORDER BY
  "{{data_table.sortOrder.column || 'id'}}" {{data_table.sortOrder.order !== "desc" ? "" : "DESC"}}
LIMIT
  {{data_table.pageSize}}
OFFSET
  {{data_table.pageOffset}}